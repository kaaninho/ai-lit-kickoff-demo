# Demo für Hector AI + Education Future Fund, Projekt "AI-LIT"

## Run the demo

Python und ein paar Librarys müssen installiert sein.

Dann das Backend via

```bash
python3 -m http.server 8000
```

starten und das Frontend via

```bash
uvicorn server:app --reload --port 3001
```

